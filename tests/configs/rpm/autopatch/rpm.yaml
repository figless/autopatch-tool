actions:

  - modify_release:
    - suffix: ".alma.1"
      enabled: true

  - changelog_entry:
      - name: "Eduard Abdullin"
        email: "eabdullin@almalinux.org"
        line:
          - "Fix: Treat x86_64_v2 as x86_64 in architecture checks"

  - add_files:
      - type: "patch"
        name: "0001-Fix-Treat-x86_64_v2-as-x86_64-in-architecture-checks.patch"
        number: 0001
